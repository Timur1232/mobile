# Key generation

```bash
keytool -genkey -v -keystore my-keystore.jks -alias my-alias -keyalg RSA -keysize 2048 -validity 10000
```

# lab1

окно регистрации/входа:

- почта
- пароль
- чекбокс согласия с условиями
- кнопки входа и регистрации

главное окно:

- вывод почты пользователя
- кнопка выхода

сохранение сессии - если закрыть приложение и открыть, то вход должен
сохраняться
